{
	"containers": [
		{
			"name": "webnull",
			"image": "ancientlore/webnull:0.1",
			"port": 8080,
			"mincpu": "50m",
			"maxcpu": "100m",
			"minmem": "20Mi",
			"maxmem": "50Mi",
			"env": [
				{ "name": "WEBNULL_CPU", "value": "1" }
			]
		}
	],
	"name": "webnull",
	"targetPort": 8080,
	"locations": [
		{
			"name": "awsqa"
		},
		{
			"name": "minikube"
		}
	],
    "clusters": 1,
    "replicas": 1,
	"user": "${KUBE_USER}",
	"token": "${KUBE_TOKEN}",
	"namespace": "${KUBE_NAMESPACE}",
	"loadBalanced": true,
	"autodeploy": true,
	"deploymentStrategy": "fast",
	"healthCheck": "tcp",
	"owner": "${KUBE_OWNER}",
	"istio": true
}
